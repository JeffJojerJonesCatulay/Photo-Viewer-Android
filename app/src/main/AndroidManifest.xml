<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exer4catulayjeffjojerjones">

    <application
        android:allowBackup="true"
        android:icon="@drawable/camicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/camicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exer4CatulayJeffJojerJones">
        <activity android:name=".ResultPhotoLibrary"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>